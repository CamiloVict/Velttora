# Frontend only (optional). SMTP lives in server/.env — see server/.env.example
# Local dev uses Vite proxy to /api/contact — no variable needed.
# Production (after Fly deploy):
# VITE_CONTACT_API_URL=https://velttora-contact-api.fly.dev/api/contact
