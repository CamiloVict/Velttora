# Vercel / production frontend (required on Vercel dashboard)
VITE_CONTACT_API_URL=https://velttora-contact-api.fly.dev/api/contact

# Local dev: omit this — Vite proxies /api/contact to localhost:3001
# SMTP secrets: server/.env and Fly only — never on Vercel
