# Secrets live in server/.fly-secrets.env (gitignored).
# Generate from local .env:  pnpm secrets:sync
# Push to Fly.io:           pnpm secrets:api
#
# Required keys:
#   SMTP_HOST, SMTP_PORT, SMTP_SECURE, SMTP_USER, SMTP_PASS
#   MAIL_FROM, CONTACT_TO, CORS_ORIGIN
